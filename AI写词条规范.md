# 藏经阁 · AI 写词条规范

> 作者 / Author：StrangeDragon1（奇怪的龙龙 / 奇怪的龍龍）　·　许可 / License：CC BY-NC 4.0（署名-非商业性使用），详见 [`LICENSE`](LICENSE)。

> 这份文档是写给 AI 看的。  
> 用途：你（AI）拿到子健给的一段故事、设定、口述碎片后，按本规范把它拆成词条，直接写成**当前世界观**文件夹下的 `.md` 文件。  
> 目标：**不用子健动手整理，AI 直接产出能进藏经阁的规范文件**，且和已有设定自动关联、不冲突、不重复。
>
> ⚠️ 本规范已按**多世界观**版本更新：所有词条不再直接放在 `data\` 下，而是放在 `世界存档\<当前世界观名>\` 里（详见第一节）。

---

## 一、三十秒理解藏经阁

```
E:\世界观\
├─ 藏经阁.exe          本地服务，双击启动（可拖文件夹指定存档）
├─ 藏经阁.html         网页界面
└─ 世界存档\           多世界观存档容器（装下所有的世界观）
   ├─ .worlds.json     世界观注册表：有哪些世界观 + 上次打开的
   └─ <当前世界观名>\    ← 你只需要动这里（词条都写进这一个）
      ├─ 人物\
      ├─ 势力\
      ├─ 物品\
      ├─ 地点\
      ├─ 事件\
      ├─ 设定\
      ├─ 地图\           地图图片（png/jpg），不是词条
      ├─ .templates.json 新建词条用的模板配置（隐藏文件，不是词条）
      ├─ .maps.json      地图上的标记位置（隐藏文件，不是词条）
      ├─ .versions\      词条的历史版本存档（隐藏文件夹）
      └─ .history.log    操作日志（隐藏文件，不是词条）
```

**先确认要写进哪个世界观**：读 `世界存档\.worlds.json` 里的 `lastActive`（或在网页顶部下拉框看当前世界观名）。词条一律写进**当前世界观**文件夹，即 `世界存档\<当前世界观名>\`。以后若切到别的世界观，就在那个世界观文件夹下写。

> **缩写约定**：下文把 `世界存档\<当前世界观名>\` 简写成 **`世界观/`**。  
> 例如 `世界观/人物/` ＝ 真正路径 `世界存档\璇星大陆\人物\`（当「当前世界观」是璇星大陆时）；  
> `世界观/.templates.json` ＝ `世界存档\<当前世界观名>\.templates.json`。

**只有六个类型文件夹里的 `.md` 才是词条。** `地图\` 放图片，点开头的都是配置或存档，**不要把词条写进这些地方**。

**四条铁律：**

1. **一个词条 = 一个 `.md` 文件，文件名就是词条名。**
2. **类型由所在文件夹决定**，文件里不要写"类型"来标注分类。
3. **正文里直接写其他词条的名字就会自动生成双向链接**，不需要 `[[]]`、`#` 之类任何标记。
4. **数据就是磁盘上的文件**，改文件 = 改数据，反之亦然。

---

## 二、词条文件长什么样

```
---
别名: 无衣，洛师妹
纪年: 宏兴25565年
境界: 元婴后期
身份: 剑冢守剑人
---

洛无衣生于剑冢旁的青石村，十九岁以血温养无名剑胚，成青莲剑。

天堕之战中她断剑三段，自此失踪。
```

**格式细节：**

- 文件开头必须是 `---`，结束也是 `---`，两行之间写字段。
- 每行一个 `键: 值`，用**第一个冒号**分割，所以值里可以再出现冒号。
- 空一行之后是正文。
- 文件编码 **UTF-8**，换行符 **LF（`\n`）**。
- 结尾保留一个换行。

**两个保留字段：**

| 字段   | 说明                          |
| ---- | --------------------------- |
| `别名` | 用逗号、顿号或中文逗号分隔。正文里提到别名也会自动关联 |
| `纪年` | 只写时间，用于编年史定位。见第六节           |

**其余全是自定义字段**，键名随意。但**不要用「类型」当字段名**——它和分类系统撞车，值为六大分类名时会被解析器丢弃。要表达类别请写「类别」「性质」。

**文件名非法字符**：`\ / : * ? " < > |` 会被替换成下划线，末尾的点和空格会被删掉。起名时主动避开。

---

## 三、拆解故事的五步

拿到一段故事，按这个顺序做：

1. **通读一遍，先列清单。** 把出现的名字（人、地方、组织、法宝、事件、概念）全部列出来，标上候选类型。
2. **扫描 `世界观/` 现有词条**（包括每个文件的 `别名`），把清单里已经存在的划掉，记下它的准确词条名——正文引用必须用它的正名或别名，否则链接不上。
3. **决定哪些该立词条**（判断标准见第四节）。
4. **逐个写文件**，按第五节的类型模板填字段。
5. **自检**（第十一节）后落盘。

**顺序建议**：先写人物和地点（其他词条都要引用它们），再写势力和物品，最后写事件。事件通常需要引用前四类。

---

## 四、该立还是不该立

**该立：**

- 有专有名字，且**后续大概率还会提到**
- 对剧情有实际作用（推动事件、绑定人物关系、构成设定规则）
- 时间线上的关键节点（事件类）

**不该立：**

- 只出现一次的路人、无名小卒、一笔带过的地名
- 纯修辞、没有具体设定的抽象词（"命运""天道"这类除非子健明确要求体系化）
- 重复概念：已有「青莲剑」就不要再立「青莲剑（残）」，写进青莲剑的正文即可

**宁少勿滥。** 空词条比没有更糟——它会污染关系图和列表。

---

## 五、六类词条的字段模板（可自定义）

**落盘前先读 `世界观/.templates.json`**，按该类型**默认模板**的字段和顺序填。文件不存在或内容损坏时，才按下面这套出厂模板填。

模板文件长这样（隐藏文件，不是词条，不会被当成内容）：

```json
{
  "version": 1,
  "templates": [
    { "id":"tpl_role_main", "name":"主角", "type":"role", "default":true,
      "fields":[ {"label":"境界","kind":"text"},
                 {"label":"年龄","kind":"number"},
                 {"label":"性别","kind":"select","options":["男","女","不详"]},
                 {"label":"门派","kind":"entry"} ] }
  ]
}
```

- `kind` 取值：`text`（文本）／`number`（数字）／`select`（下拉，选项在 `options`）／`entry`（词条引用，值必须是已存在词条的正名或别名）。
- `type` 为 `null` 表示**通用模板**，可跨类型使用。
- 字段名（label）**依然不可叫「类型」**，`kind: entry` 的值若拼错就不会自动链接。

**出厂默认模板：**

字段不必填满，**没有信息就留空或干脆不写这一行**。空字段会在界面上被隐藏。带 `→` 的字段是「词条引用」，值要填已有词条的名字。

### 人物 `世界观/人物/`

```
别名:
纪年:            ← 生卒或活跃年代，如「宏兴2500年—25565年」
境界:
年龄:            ← 数字
性别:            ← 男 / 女 / 不详
身份:
门派:            → 填势力词条名
生卒年:
父母:            → 填人物词条名，多个用逗号分隔
亲密关系:        → 同上
```

**「父母」字段会驱动「家族」页的家族树**——填了它，人物之间会自动连成谱系图，所以务必用已有人物的正名。

正文写：出身、性格、关键经历、与他人的关系、结局。**关系靠写别人的名字自动建立，不要另列关系清单。**

### 势力 `世界观/势力/`

```
别名:
纪年:            ← 建立时间
宗主:            → 人物词条名
所在地:          → 地点词条名
核心理念:
```

### 物品 `世界观/物品/`

```
别名:
品阶:
类别:            ← 注意：不要写「类型」
持有者:          → 人物词条名
来历:
```

### 地点 `世界观/地点/`

```
别名:
所属区域:        → 地点词条名
特征:
```

### 事件 `世界观/事件/`

```
别名:
纪年:            ← 事件类强烈建议写，否则进不了编年史
参与人物:        → 人物词条名，多个用逗号分隔
起因:
结果:
地点:            → 地点词条名
```

### 设定 `世界观/设定/`

```
别名:
类别:
```

用于世界观规则、修炼体系、历法、种族等抽象内容。

---

## 六、正文写作规则

1. **直接写词条名。** 想关联「洛无衣」，正文里就写"洛无衣"三个字，不要加任何标记符号。
2. **第三人称、过去时、客观陈述。** 不写"读者会觉得""这段很有趣"这类元叙述。
3. **一个段落一件事**，空行分段。
4. **不写评价性形容词堆砌**，只写事实和可验证的细节（材质、时间、地点、数量、后果）。
5. **信息密度优先**，不要为了篇幅注水。三行能说清的不要写十行。
6. 同一词条内**不要重复**已经在字段里写过的内容（比如"品阶: 上品灵剑"写了，正文就不用再说一遍它是上品灵剑）。

---

## 七、纪年与时间线

能被系统识别的写法：

```
纪年: 公元前300年3月2日
纪年: 公元300年
纪年: 宏兴25565年
纪年: 宏兴25565年5月
```

- 「公元」是内置纪元，含公元前。
- 自定义纪元（如「宏兴」）**必须登记在纪元表里**，否则排序会错。

**纪元表是特殊词条**：`世界观/设定/纪元表.md`，正文每行一个纪元名，**从上到下＝时间先后**。

```
---
---

公元
宏兴
```

引入新纪元时，务必同步更新这个文件，插到正确的位置。

无法识别的纪年（如「远古时期」）会归入「未定年」，尽量少用。

---

## 八、落盘操作规范

1. **先读后写。** 目标文件已存在时，必须先读原内容，在原有基础上补充，不要整体覆盖。
2. **新文件直接写**，文件不存在时可一步创建。
3. 类型文件夹不存在就创建（六个文件夹：人物、势力、物品、地点、事件、设定）。
4. 文件名 = 词条名 + `.md`。
5. 编码 UTF-8，换行 LF。
6. **写完告诉子健你动了哪些文件**，列出路径清单，方便他核对。

---

## 九、更新与改名

**追加**：直接编辑对应文件，在正文末尾或字段区补充。

**改名**（慎重）：改词条名意味着文件名要变，而**其他地方正文里引用的旧名字不会自动更新**，会变成死链。如果一定要改名，必须：

1. 用新名字创建新文件；
2. 全库搜索旧名字（含别名），把所有引用替换成新名字；
3. 删除旧文件。

**能不改就不改。** 需要别名时优先用 `别名` 字段解决。

---

## 十、删除一律走回收站

**不要直接删除世界观目录下的 .md 文件。** 藏经阁有回收站，删除分三步：

1. 把文件移动到 `世界观/.trash/<类型文件夹>/` 下（保留原文件名；同名已存在时改为 `名字(2).md`）。
2. 在 `世界观/.trash/index.json` 里追加一条：
   ```json
   {"name":"洛无衣","type":"role","dir":"人物","file":"洛无衣.md","deletedAt":"2026-08-29 03:55:37"}
   ```
   type 取值：role / faction / item / place / event / lore。
3. 在 `世界观/.history.log` 追加一行日志。

- 用户说"删掉某个词条"→ 走回收站，并告诉他可以在「数据」页恢复。
- 只有明确说"彻底删除""清空回收站"时，才真正删除文件。
- 恢复是反向操作：文件移回原类型文件夹，从 index.json 移除该条，日志记一行「恢复」。同名已存在时改为 `名字(2).md`。

**操作日志格式**（每次改动都追加一行，最新在最后）：

```
[2026-08-29 03:55:37] 删除 人物/洛无衣 — 已移入回收站
[2026-08-29 03:56:00] 恢复 人物/洛无衣
[2026-08-29 03:56:19] 新建 事件/天堕之战
[2026-08-29 03:56:19] 更新 物品/青莲剑
```

动作词固定用：**新建 / 更新 / 删除 / 恢复 / 彻底删除 / 改名清理旧文件**。

---

## 十一、写完后自检清单

逐条确认再交付：

- [ ] 每个新词条都放进了**正确的类型文件夹**
- [ ] 文件名 = 词条名，无非法字符
- [ ] 头部以 `---` 开始和结束，字段格式为 `键: 值`
- [ ] 没有用「类型」当字段名
- [ ] 事件类写了 `纪年`，且纪年格式可被识别
- [ ] 字段与该类型模板一致（先查 `世界观/.templates.json`，没有文件再按第五节出厂模板）
- [ ] 引用类字段（`父母`/`门派`/`持有者`/`参与人物` 等）填的是**已存在词条**的正名或别名
- [ ] 新纪元已登记进 `世界观/设定/纪元表.md`
- [ ] 正文里引用的其他词条，用的都是**已存在的正名或别名**（对照第二章的扫描结果）
- [ ] 没有和现有词条重复（同名或同义重复）
- [ ] 没有留下空壳词条（字段全空且正文为空）
- [ ] 编码 UTF-8，换行 LF
- [ ] **删除的词条已移入 `世界观/.trash` 并登记 index.json + 写日志**（没有直接删文件）
- [ ] 每次改动都在 `世界观/.history.log` 追加了一行

---

## 十二、完整示例

**输入（子健口述）：**

> 洛无衣是剑冢守剑人，元婴后期。她十九岁那年从剑冢莲池底捞起一截无名剑胚，用血养了十九年，养成青莲剑，冰属性的上品灵剑。后来天堕之战，剑碎成三段，一段被天璇盟收走，一段流落北原，最后一段下落不明。她自己也失踪了。

**输出：**

`世界观/人物/洛无衣.md`

```
---
别名:
纪年:
境界: 元婴后期
身份: 剑冢守剑人
门派:
生卒年:
---

洛无衣十九岁时在剑冢莲池底得一截无名剑胚，以血温养十九年，成青莲剑。

天堕之战中青莲剑碎为三段，她自此失踪，无人知其下落。
```

`世界观/物品/青莲剑.md`（已存在则**追加**，不覆盖）

```
---
别名:
品阶: 上品灵剑
类别: 冰属性本命剑
持有者: 洛无衣
来历: 剑冢莲池底所得，洛无衣以血温养十九年
---

青莲剑通体如凝霜，出鞘时方圆十丈飘雪。

本是剑冢深处一截无名剑胚，被洛无衣以血温养十九年，方成剑形。天堕之战中碎为三段，其中一段下落不明，成了后世许多劫难的引子。
```

`世界观/事件/天堕之战.md`

```
---
别名:
纪年:
参与人物: 洛无衣
起因:
结果: 青莲剑碎为三段，洛无衣失踪
---

天堕之战中，洛无衣所持青莲剑碎为三段，一段被天璇盟收走，一段流落北原，最后一段下落不明。

此战之后洛无衣失踪。
```

**注意示例中留空的字段**：没有信息就空着，不要编。可以在交付时告诉子健"天堕之战缺纪年，青莲剑三段的具体去向待补"，让他后续补。
